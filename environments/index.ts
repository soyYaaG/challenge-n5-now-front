export const environment = {
    api: {
        base: "http://localhost:5245/api",
        services: {
            permission: "/Permission",
            permissionTypes: "/PermissionTypes"
        }
    }
}